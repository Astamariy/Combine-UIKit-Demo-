### Headers settings
company: Combine(UIKit)

### Xcode project settings
project_name: Combine(UIKit)
xcodeproj_path: Combine(UIKit).xcodeproj

### Code generation settings section
# The main project target name
project_target: Combine(UIKit)

# The file path for new modules
project_file_path: Combine(UIKit)/Screens

### Tests generation settings section
# The tests target name
test_target: Combine(UIKit)Tests

# The file path for new tests
test_file_path: Combine(UIKit)/Scenes

### Dependencies settings section
podfile_path: Podfile

### Templates
templates:
- {name: scene}
- {name: cell}
