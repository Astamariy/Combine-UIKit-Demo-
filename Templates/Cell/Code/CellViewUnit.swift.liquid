import Foundation
import Combine

enum {{ module_info.name }}TableViewCellUnit: UnitType {
    
    enum Event {
        
    }
    
    struct Input {
        
    }
    
    struct Output {
        let empty: AnyPublisher<Void, Never>
    }
}

