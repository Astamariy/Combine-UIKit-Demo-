import Foundation
import Combine

enum {{ module_info.name }}ViewUnit: UnitType {
    
    struct Input {
        
    }
    
    struct Output {
        let empty: AnyPublisher<Void, Never>
    }
}

